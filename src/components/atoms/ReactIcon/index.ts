import {
  colorClasses,
  type IconColor,
  iconMap,
  type IconName,
  type IconSize,
  sizeClasses,
} from './lib'
import ReactIcon, { type ReactIconProps } from './ReactIcon'

export {
  colorClasses,
  type IconColor,
  iconMap,
  type IconName,
  type IconSize,
  ReactIcon,
  type ReactIconProps,
  sizeClasses,
}
